apiVersion: v1
kind: Service
metadata:
  name: wallet-frontend
  namespace: wallet
spec:
  selector:
    app: wallet-frontend
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30007
  type: NodePort
