apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: wallet
  labels:
    app.kubernetes.io/name: digital-wallet
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: digital-wallet
    app.kubernetes.io/component: frontend
  ports:
    - name: http
      port: 80
      targetPort: http
